import { Stack } from "expo-router";

export default function Services() {
  return (
    <Stack
      screenOptions={{
        headerStyle: { backgroundColor: "#121212" },
        headerTintColor: "#FFF",
      }}
    />
  );
}
